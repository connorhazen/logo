package slogo;

//world object is a shape that will exist in the view screen and is rectangular in shape (currently fixed in location and size)

public interface WorldObjectInterface {

    double getWidth();

    double getHeight();

    double getX();

    double getY();

    double getAngle();
}
